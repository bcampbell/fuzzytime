fuzzytime
=========

A date and time parsing package for Go.

Main driving force is to handle publication dates on web pages, but no
reason it can't be used on any human-readable date/time strings.

Starting off as a port of python fuzzydate library:
https://github.com/bcampbell/fuzzydate


